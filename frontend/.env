# IOsense — SERVER-SIDE auth (never shipped to the browser).
# The /api/iosense route logs in with these and proxies data calls.
IOSENSE_BASE_URL=https://connector.iosense.io/api
IOSENSE_ORG=https://iot.eneruni.com
